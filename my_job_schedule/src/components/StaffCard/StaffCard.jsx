import React from 'react'

export default function StaffCard(props) {
    return (
        <div>
                <h1>Nombre del usuario {props.name} {props.lastName}</h1>
            <div>
            </div>
        </div>
    )
}
